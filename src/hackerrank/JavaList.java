package hackerrank;

public class JavaList {
    // 2 types of queries
    // Insert y at index x
    // Delete element at index x

    //List L of N integers, perform Q queries on the list

    //first line: N (initial number of elements in L)
    //second line: N space-separated integers describing L
    //third line: integer Q(number of queries)
    // -if first line of query contains the String Insert then the second line contains
    // 2 space separated integers x y and y must be inserted into L at index x
    // -if first line of query contains the String Delete, then the second line contains index x
    // whose element must be deleted from L




}
